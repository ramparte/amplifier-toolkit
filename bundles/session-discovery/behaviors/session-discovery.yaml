bundle:
  name: behavior-session-discovery
  version: 1.0.0
  description: Session discovery and organization - auto-naming, indexing, and search

# Include the hook module
hooks:
  - module: hook-session-indexer
    source: session-discovery:modules/hook-session-indexer

# Include the agent
agents:
  include:
    - session-discovery:session-namer

# Include thin awareness context (NOT heavy docs)
context:
  include:
    - session-discovery:context/session-discovery-awareness.md
